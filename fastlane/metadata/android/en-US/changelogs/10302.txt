Fixes intermittent "Unknown propagation node" sync failures on busy meshes. Propagation nodes were being dropped from the local table by the announce-flood cleanup (which spared your contacts but not the nodes you sync from), so a sync could fail until the node announced again. Propagation nodes are now kept, and if one is briefly missing the app requests it and retries instead of failing outright.
