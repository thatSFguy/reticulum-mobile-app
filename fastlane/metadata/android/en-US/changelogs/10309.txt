Fixes the text cursor after picking a name from the mention suggestions in a Relay Chat room: it now lands at the end of the message instead of in the middle of the name that was just inserted. The same fix applies to picking a slash command from the command suggestions. Everything else in this release is test coverage — including the database repair that ran on upgrade in the previous version, which is now pinned by tests on both Android and iOS.
