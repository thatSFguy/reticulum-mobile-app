Data-loss protection. The app now keeps a last-known-good copy of its database and automatically restores it if the database is ever lost or reset (for example after a crash that leaves it corrupt) — so your messages, contacts, favorites and identity can't silently vanish on an update. The safety net is carefully guarded: it never overwrites a healthy database, and never replaces a good backup with an empty one.
